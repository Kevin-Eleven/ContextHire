# Large dataset — never commit (downloaded from the challenge bundle)
India_runs_data_and_ai_challenge/
India_runs_data_and_ai_challenge/
*.docx
.DS_Store
candidates.jsonl
candidates.jsonl.gz

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Submission output (regenerated by rank.py)
submission.csv
*.submission.csv

# Precompute artifacts: the data/*.npy files SHIP in the repo (rank.py needs them at ranking
# time, no network) — only ignore scratch .tmp files and the build log.
data/*.npy.tmp
precompute.log
Idea Submission Template _ Redrob.pptx
constraints.

problemStatement.txt
Approaches.txt