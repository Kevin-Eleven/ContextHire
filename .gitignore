# Large dataset — never commit (downloaded from the challenge bundle)
India_runs_data_and_ai_challenge/
India_runs_data_and_ai_challenge/
*.docx
.DS_Store
candidates.jsonl
candidates.jsonl.gz

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Submission output (regenerated by rank.py)
submission.csv
*.submission.csv

# Precompute caches — keep small shipped artifacts in data/ explicitly via git add -f
data/*.npy.tmp
Idea Submission Template _ Redrob.pptx
constraints.

problemStatement.txt
Approaches.txt